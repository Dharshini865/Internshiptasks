function compareStrings(str1, str2) {
   
    if (str1.length == str2.length) {
        return true;
    } else {
        return false;
    }
}
 
let result = compareStrings("hello", "world"); 
console.log (result);
